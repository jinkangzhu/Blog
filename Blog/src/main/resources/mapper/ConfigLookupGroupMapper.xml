<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.blog.mapper.ConfigLookupGroupMapper">

    <!-- 根据ID查询 -->
    <select id="findById" resultType="com.blog.entity.ConfigLookupGroup">
        SELECT
            id,
            group_code AS groupCode,
            group_name AS groupName,
            description,
            status,
            created_by AS createdBy,
            created_date AS createdDate,
            updated_by AS updatedBy,
            updated_date AS updatedDate,
            deleted_flag AS deletedFlag
        FROM config_lookup_group
        WHERE id = #{id} AND deleted_flag = 'N'
    </select>

    <!-- 插入记录 -->
    <insert id="insert">
        INSERT INTO config_lookup_group (id, group_code, group_name, description, status, created_by, updated_by, created_date, updated_date, deleted_flag)
        VALUES (#{id}, #{groupCode}, #{groupName}, #{description}, #{status}, #{createdBy}, #{updatedBy}, #{createdDate}, #{updatedDate}, #{deletedFlag})
    </insert>

    <!-- 更新记录 -->
    <update id="update">
        UPDATE config_lookup_group
        <set>
            <if test="groupCode != null">group_code = #{groupCode},</if>
            <if test="groupName != null">group_name = #{groupName},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
            <if test="createdBy != null">created_by = #{createdBy},</if>
            <if test="updatedBy != null">updated_by = #{updatedBy},</if>
            <if test="createdDate != null">created_date = #{createdDate},</if>
            <if test="updatedDate != null">updated_date = #{updatedDate},</if>
            <if test="deletedFlag != null">deleted_flag = #{deletedFlag},</if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 删除记录 -->
    <delete id="delete">
        DELETE FROM config_lookup_group WHERE id = #{id}
    </delete>

    <!-- 查询所有记录 -->
    <select id="findAll" resultType="com.blog.entity.ConfigLookupGroup">
        SELECT
            id,
            group_code AS groupCode,
            group_name AS groupName,
            description,
            status,
            created_by AS createdBy,
            created_date AS createdDate,
            updated_by AS updatedBy,
            updated_date AS updatedDate,
            deleted_flag AS deletedFlag
        FROM config_lookup_group
        WHERE deleted_flag = 'N'
    </select>
</mapper>